id = "bare-binary-adapter-no-regression"
description = "Contract for: Bare-binary adapter command string shell-split produces binary only, no args (no regression)"
archetype = "PF"
status = "active"
superseded_by = ""
authored_with = "0.1.0"

[[tests.shell]]
command = "cargo test adapter::tests::test_parse_command_string_bare_binary"
timeout_seconds = 60
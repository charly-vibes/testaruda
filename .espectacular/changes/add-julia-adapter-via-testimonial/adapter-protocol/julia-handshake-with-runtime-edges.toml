id = "julia-handshake-with-runtime-edges"
description = "Julia adapter handshake with coverage-based capabilities"
archetype = "PF"
status = "active"
superseded_by = ""
authored_with = "0.1.0"

[[tests.shell]]
command = "cargo test -p testaruda --test adapter_julia julia_adapter_handshake -- --test-threads=1"
timeout_seconds = 60

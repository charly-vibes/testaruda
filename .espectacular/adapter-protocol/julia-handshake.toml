id = "julia-adapter-handshake"
description = "Contract for: Julia adapter handshake with coverage-based capabilities (ADAPT-014)"
archetype = "PF"
status = "active"
superseded_by = ""
authored_with = "0.1.0"

[[tests.shell]]
command = "cargo test -p testaruda --test adapter_julia julia_adapter_handshake -- --test-threads=1"
timeout_seconds = 60
id = "julia-adapter-full-pipeline"
description = "Contract for: Julia adapter full protocol pipeline (handshake → discover → fingerprint → static-deps → ingest → run-args)"
archetype = "PF"
status = "active"
superseded_by = ""
authored_with = "0.1.0"

[[tests.shell]]
command = "cargo test -p testaruda --test adapter_julia julia_adapter_full_pipeline -- --test-threads=1"
timeout_seconds = 90
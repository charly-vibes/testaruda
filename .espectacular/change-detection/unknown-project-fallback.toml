id = "unknown-project-fallback"
description = "Contract for: Init falls back to a default adapter when no project type is detected"
archetype = "PF"
status = "active"
superseded_by = ""
authored_with = "0.1.0"

[[tests.shell]]
command = "grep -q 'fn detect_project_language' src/config.rs && grep -q 'else' src/config.rs && echo 'project detection fallback code exists'"
timeout_seconds = 10

[[tests.shell]]
command = "cargo test config::tests::detect_shape_missing"
timeout_seconds = 60

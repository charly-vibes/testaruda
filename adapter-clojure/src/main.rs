//! testaruda-adapter-clojure — scaffolded skeleton.
//!
//! This crate was created by ticket `testaruda-4tm` (task 1.1 of the
//! `add-clojure-adapter` openspec change). The adapter protocol commands are
//! implemented in follow-up tickets:
//!   - `testaruda-iq6` — adapter binary skeleton + JSON handshake loop
//!   - `testaruda-m7o` — Rust tree-sitter query runner + unit tests
//!   - `testaruda-o04` — `.scm` query files (discover / ns / deps)
//!   - `testaruda-fjj` — deps.edn / project.clj readers
//!   - `testaruda-cch` — config registration + language detection
//!
//! Until `testaruda-iq6` lands this binary is a no-op stub so the workspace
//! builds and `cargo test` passes on the empty crate (acceptance: 4tm).

fn main() {
    // Intentionally empty — see module docs.
}
